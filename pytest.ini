[pytest]
; Keep collection out of ingested repo checkouts under backend/data/repos —
; they carry their own conftest.py files that collide with ours.
testpaths = backend/tests
norecursedirs = backend/data .venv node_modules .git
